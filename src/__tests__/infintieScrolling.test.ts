describe('Infinite Scroll Suite', () => {
  it('Can Render 6 items when toDisplay is 6', () => {
    const toDisplay = 6;
    const itemsShown = [];
    let totalItems = 0;
    const allItems = [];
    const itemsNotShown = [];

    expect(itemsShown.length, 6);
  });
});
